{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "cloudFunctions": {
    "nodejs": {
      "includeFiles": []
    }
  },
  "services": {
    "framework": "workflow",
    "timeout": 1200,
    "sandbox": {
      "timeout": 300
    }
  },
  "agents": {
    "framework": "workflow",
    "timeout": 1200,
    "sandbox": {
      "timeout": 300
    }
  },
  "schedules": [
    {
      "name": "trends-periodic",
      "cron": "0 9 * * *",
      "path": "/trends/run",
      "payload": {
        "_schedule": true,
        "trigger": "schedule",
        "sources": ["hackernews", "devto", "web"],
        "limit": 30
      },
      "timeout": 1200
    }
  ]
}
